---
sudo: false
language: ruby
cache: bundler
rvm:
  - 2.6.1
before_install: gem install bundler -v 2.0.2

jobs:
  include:
    - stage: test
      script: bundle exec rspec
    - stage: rubocop
      scrip: bundle exec rubocop
